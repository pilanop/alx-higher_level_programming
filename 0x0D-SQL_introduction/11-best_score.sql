-- This document retrieves the 'score' and 'name' from 'second_table' where the score is greater or equal to 10, and orders the results by 'score' in descending order.
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
